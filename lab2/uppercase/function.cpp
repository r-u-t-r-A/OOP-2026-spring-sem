
void wypierdalaj() {
    vector<string> strings = {" "};
    for (int i = 0; i < 5; i++) {
        string temp;
        cin >> temp;
        strings.insert(strings.begin() + i, temp);
    }

     for (int i = 0; i < 5; i++) {
        string temp;
        cin >> temp;
        strings.insert(strings.begin() + i, temp);
    }
}